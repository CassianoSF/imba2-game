{
  "scripts": {
    "build": "imba-rollup",
    "dev": "imba-rollup --watch --serve --hmr",
    "check-version": "imbac --version"
  },
  "dependencies": {
    "imba": "^2.0.0-alpha.20",
    "imba-rollup": "^1.0.4"
  },
  "devDependencies": {}
}
